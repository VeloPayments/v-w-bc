#include <vwbc/parser.h>
#include <vpr/parameters.h>
void certificate_parser(const std::string &UNUSED(bytes), bool UNUSED(attest),
                        const emscripten::val &UNUSED(callback))
{

}
